name: Terraform CI/CD

on:
  pull_request:
    branches:
      - master

  push:
    branches:
      - master
    paths:
      - "**.tf"
      - ".terraform.lock.hcl"
      - ".tflint.hcl"
      - ".github/workflows/terraform.yml"

  workflow_dispatch:

permissions:
  contents: read

env:
  AWS_REGION: ap-northeast-1
  TF_VERSION: 1.15.1
  TF_IN_AUTOMATION: true
  TF_INPUT: false

jobs:
  lint:
    name: Terraform Lint
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}

      - name: Setup TFLint
        uses: terraform-linters/setup-tflint@v6

      - name: Terraform fmt check
        run: terraform fmt -check -recursive

      - name: Terraform init
        run: terraform init -backend=false

      - name: Terraform validate
        run: terraform validate -no-color

      - name: TFLint init
        run: tflint --init

      - name: TFLint
        run: |
          ls -la .tflint.hcl
          tflint --version
          echo "Running TFLint..."
          tflint --recursive --format compact

  plan:
    name: Terraform Plan
    runs-on: ubuntu-latest
    needs: lint
    if: github.event_name != 'pull_request' || github.actor == 'l-1cm-l'
    permissions:
      contents: read
      id-token: write
      pull-requests: write
      issues: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure AWS credentials for plan
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::169049008132:role/GitHubActionsTerraformPlanRole
          aws-region: ${{ env.AWS_REGION }}
          role-session-name: GitHubActionsTerraformPlan

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}

      - name: Terraform init
        run: terraform init

      - name: Terraform plan
        id: plan
        run: |
          set +e
          terraform plan -no-color -out=tfplan > plan.txt
          exit_code=$?
          cat plan.txt
          echo "exit_code=${exit_code}" >> "$GITHUB_OUTPUT"
          exit ${exit_code}

      - name: Comment Terraform plan on PR
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');

            const plan = fs.readFileSync('plan.txt', 'utf8');
            const maxLength = 60000;

            const truncatedPlan = plan.length > maxLength
              ? plan.substring(0, maxLength) + "\n\n... plan output was truncated ..."
              : plan;

            const body = `## Terraform Plan Result

            **Repository:** \`${{ github.repository }}\`  
            **Branch:** \`${{ github.head_ref }}\`  
            **Workflow:** \`${{ github.workflow }}\`  
            **Commit:** \`${{ github.sha }}\`

            <details>
            <summary>Show terraform plan</summary>

            \`\`\`terraform
            ${truncatedPlan}
            \`\`\`

            </details>
            `;

            const { owner, repo } = context.repo;
            const issue_number = context.issue.number;

            const comments = await github.rest.issues.listComments({
              owner,
              repo,
              issue_number,
              per_page: 100
            });

            const marker = '## Terraform Plan Result';
            const existingComment = comments.data.find(comment =>
              comment.user.type === 'Bot' && comment.body.includes(marker)
            );

            if (existingComment) {
              await github.rest.issues.updateComment({
                owner,
                repo,
                comment_id: existingComment.id,
                body
              });
            } else {
              await github.rest.issues.createComment({
                owner,
                repo,
                issue_number,
                body
              });
            }

  apply:
    name: Terraform Apply
    runs-on: ubuntu-latest
    needs: plan
    permissions:
      contents: read
      id-token: write

    if: |
      github.event_name == 'push' && github.ref == 'refs/heads/master' ||
      github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/master'

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure AWS credentials for apply
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::169049008132:role/GitHubActionsTerraformApplyRole
          aws-region: ${{ env.AWS_REGION }}
          role-session-name: GitHubActionsTerraformApply

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}

      - name: Terraform init
        run: terraform init

      - name: Terraform plan for apply
        run: terraform plan -no-color -out=tfplan

      - name: Terraform apply
        run: terraform apply -auto-approve -no-color tfplan
